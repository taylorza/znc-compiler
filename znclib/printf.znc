#ifndef PRINTF__
const byte PRINTF__=0x01;

include "format.znc"

void _printf_sink(char ch, void* ctx) {
  putc(ch);
}

void printf(char *fmt, ...) {
  va_list args;
  va_start(args, fmt);
  vsprintf(_printf_sink, 0, fmt, args);
  va_end(args);
}
#endif